# v0.5 Step 09 - Full UAT

## Goal

Prove the MVP can survive realistic song-entry flows before the final
checkpoint.

## Result

- Added automated user-acceptance tests for the treble melody.
- Added automated user-acceptance tests for the piano grand-staff exercise.
- Covered realistic mistakes in UAT:
  - skipped note repaired with Insert mode
  - wrong note repaired by dragging
  - extra note removed through the notation delete target
- Verified final note order and durations for both songs.
- Fixed drag-test parity so the simulated drag emits the same suppressing click
  sequence a real browser produces after mouseup.
- Kept moved notes active but safe from accidental toolbar duration edits.

## Browser QA

Passed:

- Reloaded the backend-enabled app.
- Switched to Grand staff piano.
- Placed a treble note and a bass whole note.
- Started playback with both notes present.
- Exported PDF and verified status changed to `PDF downloaded`.

## Tests

Passed:

```txt
npm.cmd run test
14 files passed, 106 tests passed
```

Passed:

```txt
npm.cmd run typecheck
```

Passed:

```txt
npm.cmd run build
```

Build note:

- Vite still warns about large chunks. This remains a later code-splitting task.

## Checkpoint

Step 09 passes. The MVP now has end-to-end user-flow coverage for treble entry,
piano entry, correction flows, playback smoke, and backend PDF export.
