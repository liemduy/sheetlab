# v0.6 Step 07 - Final UAT

## Goal

Run user-like acceptance coverage across the upgraded editor before closing the
v0.6 interaction pass.

## Result

- Updated UAT flows to use real delete clicks on the small `x`.
- Treble melody UAT covers:
  - note entry
  - insert missing note
  - drag repair
  - delete correction
  - mixed quarter/half durations
- Piano grand-staff UAT covers:
  - treble melody entry
  - bass whole-note accompaniment
  - insert/delete correction
  - drag repair
- PDF UAT covers:
  - create a short grand-staff score
  - call backend export endpoint from the app
  - receive a PDF blob and trigger download

## Browser And Backend Smoke

Passed on `http://localhost:5173/`:

- Browser renderer count: 1.
- Browser UI created 1 score event.
- Browser UI showed 1 delete control.
- Browser UI delete returned event count to 0.
- Real backend `/api/export-pdf` returned a PDF file:
  - first bytes: `%PDF-`
  - length: 1965 bytes

## Tests

Passed:

```txt
npm.cmd run test
15 files passed, 136 tests passed
```

Passed:

```txt
npm.cmd run typecheck
```

Passed:

```txt
npm.cmd run build
```

Build note:

- Vite still warns about large chunks. This remains a later code-splitting task.

## Checkpoint

Step 07 passes. v0.6 now has tested slot entry, snapped placement, usable
delete, wider ledger range, cleaner sheet visuals, piano UAT, and real PDF
export smoke coverage.
