# V0.2 Checkpoint 05: VexFlow Adapter

## Step Goal

Create a renderer adapter that maps internal score events to VexFlow-friendly note specs.

## Expected Result

- Pitch values map to VexFlow keys like `c/4`.
- Duration values map to VexFlow duration strings.
- Rests map to VexFlow rest durations and clef-aware rest keys.
- Accidentals map to VexFlow symbols.

## Tests

- Run `npm.cmd run test`.
- Run `npm.cmd run typecheck`.
- Run `npm.cmd run build`.

## Pass Criteria

- Adapter unit tests pass.
- Existing tests remain green.
- TypeScript remains green.
- Production build remains green.

## Result

- Added `src/features/sheet/vexflowAdapter.ts`.
- Added adapter tests for pitch, duration, rest, and accidental mapping.
- `npm.cmd run test` passed with 11 test files and 47 tests.
- `npm.cmd run typecheck` passed.
- `npm.cmd run build` passed.

## Status

Passed on 2026-05-06.
