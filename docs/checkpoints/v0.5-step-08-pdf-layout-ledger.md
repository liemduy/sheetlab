# v0.5 Step 08 - PDF Layout And Ledger

## Goal

Make PDF layout testable and protect ledger-line export from clipping.

## Result

- Added a PDF layout plan from the same geometry used to draw the PDF.
- Added PDF ledger stress coverage for:
  - Treble A3, C4, A5, C6
  - Bass A1, C2, C4, E4
- Verified PDF notehead, stem, and ledger bounds stay inside the printable page
  safe area.
- Generated a real stress PDF at `.codex-temp/ledger-stress.pdf`.
- Restarted the backend dev server and verified the UI export still downloads a
  PDF.

## Browser QA

Passed:

- Reloaded the backend-enabled app.
- Clicked `Export PDF` from the visible UI.
- Verified status changed to `PDF downloaded`.

## Tests

Passed:

```txt
npm.cmd run test
13 files passed, 104 tests passed
```

Passed:

```txt
npm.cmd run typecheck
```

Passed:

```txt
npm.cmd run build
```

Build note:

- Vite still warns about large chunks. This remains a later code-splitting task.

## Checkpoint

Step 08 passes. PDF export has explicit layout and ledger clipping regression
coverage.
