# v0.6 Step 06 - Sheet Polish

## Goal

Make the editor surface look more like a real sheet page and less like a debug
canvas.

## Result

- Removed the old `V0.4 notation surface` instruction pill from the paper.
- Removed floating `Treble`/`Bass` hover labels from the staff surface.
- Made hover highlight, rhythm slots, and the active input cursor lighter and
  less visually noisy.
- Kept rhythm slots visible enough for MuseScore-style entry while preserving a
  cleaner sheet-music look.

## Browser QA

Passed on `http://localhost:5173/`:

- 1 notation renderer exists.
- Old `V0.4 notation surface` text count is 0.
- Floating `Bass` debug label count is 0.
- Quarter slot count remains 16 on a 4-measure treble score.

## Tests

Passed:

```txt
npm.cmd run test
15 files passed, 135 tests passed
```

Passed:

```txt
npm.cmd run typecheck
```

Passed:

```txt
npm.cmd run build
```

Build note:

- Vite still warns about large chunks. This remains a later code-splitting task.

## Checkpoint

Step 06 passes. The paper surface is cleaner and closer to a real sheet while
keeping the new slot-entry affordances.
