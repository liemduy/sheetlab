# v0.5 Step 03 - Ledger Safe Area

## Goal

Make ledger-line rendering a hard gate instead of a visual guess.

## Result

- Added automated stress coverage for treble and bass ledger notes.
- Covered both placed notes and ghost notes.
- Verified expected ledger line counts for:
  - Treble A3, C4, A5, C6
  - Bass A1, C2, C4, E4
- Verified notehead/stem/ledger y-values stay within the SVG viewBox.
- Increased vertical pointer tolerance so users can target required ledger notes.

## Browser QA

Passed:

- Reset treble score and returned to notation area.
- Placed high treble ledger note without clipping.
- Placed low treble ledger note without clipping.
- Switched to grand staff.
- Verified bass ledger ghost appears in the visible grand staff.
- Placed a bass note in grand staff from the visible browser surface.

The exact full ledger stress matrix is covered by automated tests because the
narrow in-app viewport makes some bass stress clicks visually cramped.

## Tests

Passed:

```txt
npm.cmd run test
11 files passed, 86 tests passed
```

Passed:

```txt
npm.cmd run typecheck
```

Passed:

```txt
npm.cmd run build
```

Build note:

- Vite still warns about large chunks. This remains a later code-splitting task.

## Checkpoint

Step 03 passes. Ledger-line rendering is now protected by explicit regression
tests for treble and grand staff.
