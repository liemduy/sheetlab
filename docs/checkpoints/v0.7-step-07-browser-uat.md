# v0.7 Step 07 - Browser UAT

## Goal

Use the running app like a real user and verify the editor behavior that used
to feel wrong: hover/click alignment, add/delete/insert, piano grand staff,
playback, and backend PDF export.

## Result

- Reloaded `http://localhost:5173/` in the in-app browser.
- Created a treble chord by clicking two pitches in the same rhythmic slot.
- Deleted the selected chord using the small `x` target.
- Used insert mode to add a note between existing notes and shift later notes.
- Switched to grand staff piano and verified shared treble/bass timeline
  columns are visible.
- Started and stopped playback without console errors.
- Exported PDF through the backend endpoint and observed `PDF downloaded`.
- Re-tested the old ghost/click bug by comparing SVG coordinates:
  `ghost cx/cy = 154/145.5`, placed note `cx/cy = 154/145.5`.

## Tests

Passed before browser UAT:

```txt
npm.cmd run test
18 files passed, 163 tests passed
```

Passed before browser UAT:

```txt
npm.cmd run typecheck
```

Browser UAT passed:

```txt
hover ghost and placed note aligned: true
playback smoke: started/stopped
PDF export smoke: PDF downloaded
console errors: []
```

## Checkpoint

Step 07 passes. The main editor loop now behaves coherently enough for the next
iteration: hover, click, chord, delete, insert, piano columns, playback, and PDF
export all survived a real browser pass.
